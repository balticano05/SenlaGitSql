version: '3.9'

services:

  db:
    image: postgres:latest
    restart: always
    environment:
      - POSTGRES_DB=db
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=mystrongpassword
    ports:
      - "8080:5432"

volumes:
  postgresql_volume: